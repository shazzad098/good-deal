import React from 'react';

const Alert = () => {
    return (
        <div className="alert-container">
            {/* Alert component - you can add alert functionality later */}
        </div>
    );
};

export default Alert;